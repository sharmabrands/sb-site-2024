{
  "name": "sb-site-2024",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve",
    "build": "node builder.js"
  },
  "dependencies": {
    "esbuild": "^0.20.2",
    "serve": "^14.2.1"
  }
}
